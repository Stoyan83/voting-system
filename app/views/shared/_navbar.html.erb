<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><%= t('navbar.voting_system') %></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="<%= t('navbar.toggle_navigation') %>">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item active">
          <a class="nav-link" href="/"><%= t('navbar.home') %></a>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item dropdown">
          <a class="nav-link" href="#" id="profileDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <% if user_signed_in? %>
              <span class="d-inline-flex justify-content-center align-items-center rounded-circle" style="background-color: #6f42c1; width: 30px; height: 30px; font-size: 14px; color: white;">
                <%= current_user.email[0].upcase %>
              </span>
            <% else %>
              <i class="bi bi-person-circle" style="font-size: 30px; width: 30px; height: 30px;"></i>
            <% end %>
          </a>
          <ul class="dropdown-menu dropdown-menu-center p-0" aria-labelledby="profileDropdown" style="min-width: 80px; font-size: 14px; ">
            <% if user_signed_in? %>
              <li>
                <%= button_to t('navbar.sign_out'), destroy_user_session_path, method: :delete, class: 'dropdown-item py-1 px-2' %>
              </li>
            <% else %>
              <li>
                <a class="dropdown-item py-1 px-2" href="<%= new_user_session_path %>"><%= t('navbar.sign_in') %></a>
              </li>
            <% end %>
          </ul>
        </li>
        <li class="nav-item d-flex align-items-center ms-0 ms-sm-4">
          <div class="form-check form-switch">
            <input class="form-check-input" type="checkbox" id="switch-view-toggle">
            <label class="form-check-label" for="switch-view-toggle"><%= t('navbar.modern_view') %></label>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>